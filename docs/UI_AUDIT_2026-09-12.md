# UI/UX 一致性审计报告（2026-09-12）

三路并行审计：设置域（35 页）、聊天域（HomeScreen + ui/chat 31 文件 + 抽屉）、助手编辑域 + 供应商/模型域（29 文件），对照基线（IosWidgets / SettingsUi / MemoTopBar / ChatStyleSpec / 各 Flutter 源页）。**只审计，未改任何代码。**行号以当日文件为准。

**整体结论**：顶栏/触感/反馈/图标体系大体健康（0 裸 Toast、全站 Snackbar、状态栏 insets 全覆盖、破坏性钮统一 error 色）；漂移集中在四类结构性问题——**M3 默认样式残留**（sheet/对话框没改圆角底色）、**共享件被绕开**（拖柄/组头/顶栏钮/字段各自手写）、**insets 半数缺失**、**触感覆盖补丁化**。供应商/模型域是重灾区。

---

## 高严重度

### H1. M3 默认样式残留（sheet ≈14 处 + AlertDialog ≈34 处，28dp 圆角 + 非 overlaySurface 底，与全站 16dp + overlaySurface 并存）
- **sheet（14）**：SettingsScreen:332、BehaviorStartupSettingsScreen:419、DisplaySettingsScreen:416/454/500/527/634（5 个）、MemoryTraceScreen:213、NetworkProxyScreen:301、AboutScreen:305、TtsServicesScreen:311/579、LogViewerScreen:1859、**SettingsUi.kt:304（共享 LanguageSheet 本体）**、ProviderSubPages.kt:274。
- **AlertDialog（34）**：设置域 13 处（MessageStyleSettingsScreen:505/540、MemorySettingsScreen:377、StorageSpace.kt:1079、LocalSnapshotsScreen:651、McpScreens.kt:177、SearchServiceEditorScreen:639、InstructionInjectionScreen:181、WorldBookScreen:305、BackupScreen:788/855、BackupReminderUi.kt:552）；聊天域 8 处（MessageSheets.kt:287-322 重生成确认、HomeScreen.kt:2377-2398 内联重生成确认〔与前者重复实现且文案资源不同〕、ReasoningBudgetSheet.kt:215-244、ToolCallCard.kt:921-961 审批拒绝〔破坏性确认钮未用 error〕、CompressContextDialog.kt:347-360、SideDrawerContent.kt:977/1009/1112/1213）；供应商域 13 处（ProviderDetailScreen.kt:394/1206/1229、ProviderListScreen.kt:420、MultiKeyManagerScreen.kt:433、ProviderGroupsScreen.kt:210/315、ProviderSheets.kt:879、ProviderSettingsScreens.kt:241、TagsManagerScreen.kt:175/229、AssistantEditMemoryTab.kt:624、ProviderAvatarSheet.kt:127/196）。
- 合规对照：backup 对话框三件套（BackupImportModeDialog/BackupRestartDialog/BackupTaskRunner，r16+overlaySurface）、ConnectionTestDialog。
- **修法建议**：收敛一个 `MemoAlertDialog` / `MemoSheet` 共享包装器一次收全部。

### H2. sheet 背景色分裂：cs.surface vs semantic.overlaySurface 两阵营
- cs.surface 阵营（漂移）：聊天域 10 处（BottomToolsSheet.kt:76〔与 ContextManagementSheet.kt:56 同流跳变〕、MessageSheets.kt:88/195、LanguageSelectSheet.kt:91、CitationSourcesSheet.kt:488、ModelSelectSheet.kt:206〔**直接源偏差**，Flutter 源明确 overlaySurface〕、SideDrawerContent.kt:841/1044/1146、MiniMapSheet.kt:141、SearchSettingsSheet.kt:80、OcrPromptSheet.kt:68）+ 供应商域 8 处（ProviderDetailScreen.kt:961〔源偏差〕、ProviderSheets.kt:454/610、MultiKeyManagerScreen.kt:375/647/699、ImportProviderSheet.kt:314、AssistantEditMemoryTab.kt:547）+ 设置域 4 处（McpScreens.kt:360/743/870、SearchServicesScreen.kt:413）。

### H3. insets 缺失/不当（交互断裂）
- **多选底部操作栏**（ExportBar/DeleteBar，ChatSelectionUi.kt:61-65/120-124）无 navigationBars/IME insets——同屏输入栏有，手势条压按钮。
- **表单页无 imePadding**：WebDavSettingsScreen（5 输入 0 处理）、NetworkProxyScreen、SearchServiceEditorScreen 主页（其子页反而有）；供应商域 ImportProviderSheet.kt:318-321、ProviderSheets.kt:235-238（AddProviderSheet）。
- **ImageViewer** 固定 padding 替代 insets（:297 顶 34dp、:326 底 28dp）。
- ModelSelectSheet 内嵌搜索框无 imePadding（低，键盘遮 provider chips）。

### H4. 助手侧 5 个显示开关全部未接线（设置可切、聊天永不生效）
`display_show_model_name_v1` / `display_show_model_timestamp_v1` / `display_show_user_message_actions_v1` / `display_show_token_stats_v1` / `display_show_provider_in_chat_message_v1`（ChatItemDisplaySettingsScreen.kt:86-96 定义；HomeScreen.kt:1997-2013 名字/时间戳无条件渲染、:2244 用户操作行不读开关、:2342-2353 TokenDisplay 只看 totalTokens）。**应并入 PORTING §5.12 渲染批**。

### H5. Material Icons 误用（全站仅 2 处）
- ModelSelectSheet.kt:24-26/487 `Icons.Filled.Favorite/FavoriteBorder`（Lucide.Heart 已 import 未用）。
- AssistantEditPromptTab.kt:43-44 `Icons.Filled.FileOpen`。

### H6. 硬编码字符串进用户可见 UI
- BackupScreen.kt:186/243/244/248（中文错误文案）、:901-903（reportDetails 拼「设置项:/实体:/会话已恢复」）。
- McpScreens.kt:187（"MCP server deleted"）、LogViewerScreen.kt:497/677（"Export failed: …"）。
- ProviderListScreen.kt:440（"Deleted"）、ImportProviderSheet.kt:301（"QR not detected"）。
- ToolResults.kt:84/86（天气卡片 "feels …°C"/"% precip"）、ImageViewer.kt:227（"share failed"）。

---

## 中严重度（规格漂移）

| # | 主题 | 明细 |
|---|---|---|
| M1 | AlertDialog 取消钮落 M3 primary | LocalSnapshotsScreen:664、McpScreens:196、MessageStyle:513/542、SearchServiceEditor:654、InstructionInjection:196、MemorySettings:414、WorldBook:317、StorageSpace:1120（基线 onSurface@74%） |
| M2 | sheet 标题字号/对齐漂移 | 15/16/17/18sp 四档并存；TtsServices:316/584、LogViewer:1866、MemoryTrace:219 左对齐 vs 其余居中 |
| M3 | sheet 顶圆角 16/20/22/28 混用 | 16dp 一侧无上游依据注释；McpScreens 同文件 20/16 自相矛盾 |
| M4 | 选项行底色 5 种写法 / 选项行 3 种样式 | surfaceCard（规范）/surfaceCardFill/surfaceContainerHigh@0.6/surfaceVariant@0.5/分层特例；GroupPickerTile、MemoryChoiceSheet 裸行 |
| M5 | 触感覆盖补丁化（约 30 处缺失 + 4 处显式关闭） | 同类 sheet 选项行 ReasoningBudget 有 / LanguageSelect·MoreAction·抽屉 MenuRow 无；建议气泡、版本切换、引用卡、多选全选/反选、Translate 整页 0 处；TtsServices/AsrServices 4 处 `haptics=false` 绕开用户开关；分段条两个实现均丢失 Flutter 的 _TactileRow 触感 |
| M6 | 按压反馈死代码 | ProviderListScreen IconActionButton/GlassCircleButton、BalanceScreen 查询钮——注释声明 alpha/scale 反馈但 pressed 永不置 true |
| M7 | 文本框 6+ 套规格 | IosFormField 之外，OutlinedTextField 本地实现 6 族（圆角 10/12/14、底色 3 种、聚焦边框 3 档），跨页肉眼可辨；WebDav 表单 vs SearchService 编辑器表单差异明显 |
| M8 | 顶栏 action 4 种实现 + 顶栏分裂 | TopBarAction（规范）/IconActionButton（ripple）/IconButton44（ripple）/裸 IosIconButton（34dp 命中）；StorageSpace 用 M3 IconButton 24dp；ProviderSubPages/ProviderGroups 自绘顶栏 vs MemoTopBar |
| M9 | 分段条两套互相漂移 | EditSegTabBar（对齐源）vs ProviderSheets SegTabBar（暗色壳色漂移、选中 SemiBold、均分无 88dp）；两者都丢了触感 |
| M10 | 组卡两套 + 组头 7 个私有变体 | SectionCard（12 页）vs SettingsSectionCard（10 页）填充/描边不同；SectionHeader 间距节奏漂移（first/非 first 2/12/6 vs 恒 0/18） |
| M11 | 拖柄 8+ 份手写拷贝 | SettingsUi.MemoSheetHandle 成摆设；上下留白 12/14/16 不一 |
| M12 | divider 几何漂移 | BackupDivider 无 6dp 槽无 54dp 内缩；SearchServices 无槽；TtsSettings start14 |
| M13 | 空态 3-4 种风格 | 13sp@0.45-0.6 / bodyMedium@onSurfaceVariant / 15sp@0.68+72dp 图标 / M3 默认 16sp |
| M14 | 裸排说明文字 | NetworkProxyScreen:212-216、LocalSnapshotsScreen:292/340（基线：BadgeInfo+Tooltip） |
| M15 | LogViewer/Debug 私有卡片体系 | 边框 alpha 0.26/0.38 重于全站 hairline；两页互不一致 |
| M16 | RegenerateConfirm 双实现 | MessageSheets 与 HomeScreen 各一份、文案资源不同 |
| M17 | 裸 clickable 用 M3 ripple | QuickPhrase/Regex 行、ProviderList M3 Card、ProviderGroups CardIconButton（基线无 ripple） |

## 低严重度（摘要）

- 魔法数：助手正文 15.7/23.55 未进 ChatStyleSpec；QuickPhraseMenu `bottom=122dp`。
- 陈旧注释：HomeScreen.kt:726-733「必须 readLocal」与现实现（readJson）矛盾，会误导后续修改。
- 死代码：ProviderEditScreen（ProviderSettingsScreens.kt:96-259 无调用点）、IconButton44 未用、DefaultModelScreen 顶栏 Spacer 残留。
- 硬编码 a11y contentDescription 英文（HomeScreen 消息操作行、ProviderSheets 等 ~15 处）。
- 硬编码英文行标题（"Temperature"/"Top P"/"API Key" 等——与 Flutter 源一致，属忠实移植但违反字串规范，需加 ARB）。
- 圆角两族 r14/r16 混用；chevron 强度不一（0.35/0.4/0.9）；禁用发送钮仍可点（有涟漪无动作）；重复 import。
- navigationBars insets 全域仅 AsrServicesEditor 一处显式处理（异类）。

---

## 共性根因与修法建议（未动手）

1. **缺共享封装**：`MemoAlertDialog` / `MemoSheet`（统一 r16+overlaySurface+拖柄+标题）能一次收敛 H1/H2/M1/M2/M3/M11 约 60 处。
2. **顶栏 action 收口**为 TopBarAction 单一实现（M7/M8）。
3. **触感按「行/卡片/气泡」三类收口**到 TactileRow/IosSheetButton 体系（M5 + 供应商域 Z3/Z4）。
4. **表单统一 IosFormField**（M7/Z5），或在共享件上加 visualTransformation/trailing 已有的扩展继续补齐。
5. 助手侧 5 个显示开关（H4）并入 §5.12 渲染批一起接线。
6. 硬编码字符串进 ARB（H6 + 低项的行标题）。
